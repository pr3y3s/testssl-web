services:
  - type: web
    name: testssl-web
    env: python
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y testssl
      pip install -r requirements.txt
    startCommand: python app.py
